diff --git a/template.yaml b/template.yaml
index 1a2b3c4..5d6e7f8 100644
--- a/template.yaml
+++ b/template.yaml
@@ -15,8 +15,8 @@ Resources:
     Properties:
       CodeUri: src/handlers/process-orders/
       Handler: index.handler
-      MemorySize: 256
-      Timeout: 30
+      MemorySize: 1024
+      Timeout: 120
       Runtime: nodejs20.x
       Architectures:
         - arm64
@@ -30,6 +30,14 @@ Resources:
             Properties:
               Path: /orders/process
               Method: post
+        SQSEvent:
+          Type: SQS
+          Properties:
+            Queue: !GetAtt OrderQueue.Arn
+            BatchSize: 10
+            MaximumBatchingWindowInSeconds: 5
+      ReservedConcurrentExecutions: 50
+      ProvisionedConcurrencyConfig:
+        ProvisionedConcurrentExecutions: 5

   GetOrderFunction:
     Type: AWS::Serverless::Function
@@ -42,6 +50,62 @@ Resources:
       Events:
         GetOrder:
           Type: Api
           Properties:
             Path: /orders/{id}
             Method: get

+  AnalyticsIngestionFunction:
+    Type: AWS::Serverless::Function
+    Properties:
+      CodeUri: src/handlers/analytics-ingest/
+      Handler: index.handler
+      MemorySize: 512
+      Timeout: 60
+      Runtime: nodejs20.x
+      Architectures:
+        - arm64
+      Environment:
+        Variables:
+          DYNAMODB_TABLE: !Ref AnalyticsTable
+          KINESIS_STREAM: !Ref AnalyticsStream
+      Policies:
+        - DynamoDBCrudPolicy:
+            TableName: !Ref AnalyticsTable
+        - KinesisStreamReadPolicy:
+            StreamName: !Ref AnalyticsStream
+      Events:
+        KinesisEvent:
+          Type: Kinesis
+          Properties:
+            Stream: !GetAtt AnalyticsStream.Arn
+            StartingPosition: TRIM_HORIZON
+            BatchSize: 100
+
+  AnalyticsTable:
+    Type: AWS::DynamoDB::Table
+    Properties:
+      TableName: !Sub "${AWS::StackName}-analytics"
+      BillingMode: PAY_PER_REQUEST
+      AttributeDefinitions:
+        - AttributeName: pk
+          AttributeType: S
+        - AttributeName: sk
+          AttributeType: S
+      KeySchema:
+        - AttributeName: pk
+          KeyType: HASH
+        - AttributeName: sk
+          KeyType: RANGE
+      Tags:
+        - Key: Team
+          Value: analytics
+
+  AnalyticsStream:
+    Type: AWS::Kinesis::Stream
+    Properties:
+      Name: !Sub "${AWS::StackName}-analytics-stream"
+      ShardCount: 4
+      StreamModeDetails:
+        StreamMode: PROVISIONED
+
+  OrderQueue:
+    Type: AWS::SQS::Queue
+    Properties:
+      QueueName: !Sub "${AWS::StackName}-order-queue"
+      VisibilityTimeout: 300
+      MessageRetentionPeriod: 1209600
+      RedrivePolicy:
+        deadLetterTargetArn: !GetAtt OrderDLQ.Arn
+        maxReceiveCount: 3
+
+  OrderDLQ:
+    Type: AWS::SQS::Queue
+    Properties:
+      QueueName: !Sub "${AWS::StackName}-order-dlq"
+      MessageRetentionPeriod: 1209600
diff --git a/src/handlers/analytics-ingest/index.js b/src/handlers/analytics-ingest/index.js
new file mode 100644
index 0000000..abcdef1
--- /dev/null
+++ b/src/handlers/analytics-ingest/index.js
@@ -0,0 +1,35 @@
+const { DynamoDBClient } = require("@aws-sdk/client-dynamodb");
+const { DynamoDBDocumentClient, PutCommand } = require("@aws-sdk/lib-dynamodb");
+
+const client = new DynamoDBClient({});
+const docClient = DynamoDBDocumentClient.from(client);
+
+exports.handler = async (event) => {
+  const records = event.Records.map((record) => {
+    const payload = Buffer.from(record.kinesis.data, "base64").toString();
+    return JSON.parse(payload);
+  });
+
+  const writePromises = records.map((record) =>
+    docClient.send(
+      new PutCommand({
+        TableName: process.env.DYNAMODB_TABLE,
+        Item: {
+          pk: `EVENT#${record.eventType}`,
+          sk: `${record.timestamp}#${record.eventId}`,
+          ...record,
+          ttl: Math.floor(Date.now() / 1000) + 90 * 24 * 60 * 60,
+        },
+      })
+    )
+  );
+
+  await Promise.all(writePromises);
+
+  return {
+    statusCode: 200,
+    body: JSON.stringify({ processed: records.length }),
+  };
+};
